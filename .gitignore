all_output.txt
result
.flatpak-builder/
audit.md
engine/build/
# Out-of-tree CMake build directories (engine/build-web, engine/build-win64, ...)
engine/build-*/
# Assembled static PWA artifact (scripts/wasm-assemble-artifact.sh)
/dist/
*.o
*.d

# bv (beads viewer) local config and caches
.bv/
.beads/
/capture.rdc

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
/image4.png

# Beads / Dolt files (added by bd init)
.beads/proxieddb/
